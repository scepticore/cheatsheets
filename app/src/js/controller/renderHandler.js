export function renderMarkdownToPdf() {
  const renderButton = document.getElementById("renderpreview");

  renderButton.addEventListener("click", function (event) {
    // Call pdf-generator service

    // Open PDF-Link in new tab
  });
}